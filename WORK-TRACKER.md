# Work Tracker

One place to see every open task across the whole repo, so nothing gets
lost across chats, roles, or people. The Manager (root `CLAUDE.md`) owns
this file.

## Rules
1. Any open item (decision needed, unfinished work, gap, blocked task)
   gets a line here **in the same session it's found**, same time it's
   noted in that project's own `Memory.md`.
2. Check this file at the start of any session before starting work.
3. Delete or check off a line the moment it's resolved. Don't let this
   file go stale; it's only useful if it's current.
4. Format: `- [ ] item (path/to/project, flagged YYYY-MM-DD)`
5. **`Ideas/` folders are excluded: never add items from there.** Not
   active work yet; the Manager surfaces those separately, on request,
   when looking for new work to start.
6. Items in a role's `Review ToDo/` folder go here too. Once Karen approves
   one, move it wherever it actually belongs (not always `Ready/`) and
   remove its line here.
7. Check every role's `Review ToDo/` folder at the start of any session
   (not just the ones already listed below); a file sitting there with no
   line here means this tracker has gone stale and rule 3 was missed.
8. **Scheduled-automation staleness check.** At the start of any session,
   check whether registered scheduled automations actually fired on
   schedule. Currently tracked: the daily brief (`state/last-run.log` under
   `Team/CEO/In Progress/Set Up Daily Housekeeping/`, expected every
   morning) and the Apple Opportunity Radar (`state/last-run.log` and
   `state/monthly-refresh-log.json` under `Team/CMO/In Progress/App Store
   Specialist/Apple Opportunity Radar/`, expected nightly + monthly once
   its scheduled tasks are registered; see that project's `CLAUDE.md` for
   whether that's confirmed yet). If a run is stale beyond its expected
   cadence, flag it to Karen immediately and offer to trigger a manual
   re-run in that session; don't wait for her to notice.

## CEO: Daily Brief (Set Up Daily Housekeeping)

Surfaced by the 2026-09-06 and 2026-09-07 scheduled runs. Project:
`Team/CEO/In Progress/Set Up Daily Housekeeping/`.
- [ ] **Run script still does not fail loudly when auth expires.** The Sep 2 to Sep 5 gap (Sep 5 exit code 1, "Failed to authenticate: OAuth session expired and could not be refreshed"; Sep 2 to Sep 4 started but logged no completion line) went unnoticed because nothing alerted Karen. Auth itself is fine again: the 2026-09-07 run completed end to end. What remains is the alerting gap, `run-daily-brief.ps1` needs to surface a failed or half-finished run instead of leaving a silently missing brief. Flagged 2026-09-06, narrowed 2026-09-07
- [x] **HTML sent as literal escaped text. Closed 2026-09-10: the rule is now written into the skills.** Kari's rollup 2026-09-06 (two emails, the first unreadable); the Park Employee Email reply draft 2026-09-07 (caught in-run, nothing shipped); Kari's rollup again 2026-09-08 (two emails again, the second telling her to ignore the first). Clean on 2026-09-09 and clean again on 2026-09-10, both confirmed by read-back from Sent Items, one send each, no correction email. This stayed open because prevention lived in the caller, not the skill. **Fixed 2026-09-10**: `daily-brief-email-triage` now has an "HTML bodies: pass raw tags, then read back" section covering every draft and every send, and `daily-brief-compose` §4 carries the same rule for Karen's own brief. Both require raw unescaped tags AND the HTML body type AND a read-back before moving on. Flagged 2026-09-06, recurred 2026-09-07 and 2026-09-08, clean 2026-09-09 and 2026-09-10, closed 2026-09-10
- [ ] **Recurring, still unresolved:** Kari's inbox-activity delta can't trace items that left the Inbox (Outlook reissues the message id on folder move, known since 2026-08-22, confirmed again 2026-09-06). The Taylor Estates half of this item is now closed, see the next line. Flagged 2026-09-06
- [x] **Make the oversized-email recovery trick a standing rule in the skill.** The Aug 12 "FW: WJ & Side Management : Taylor Estates Project General Updates" email (166,815 chars, over the read tool's limit) had been reported unreadable on every run since Aug 21. The 2026-09-08 run recovered its full text from the saved tool-result file, and it turned out to contain a live 27-day-old decision waiting on Karen (pick Concept A or B for the Taylor Estates website). `daily-brief-email-triage` should do this by default for any body over the read limit instead of reporting a permanent gap. Added 2026-09-08. **Worked again 2026-09-09** (full Taylor Estates thread recovered a second time), still only because the caller asked for it. Not yet in the skill. **Done 2026-09-10**: `daily-brief-email-triage` now has an "Oversized message bodies: recover, do not write off" section requiring the saved tool-result file be read before anything is reported as unreadable. Closed 2026-09-10
- [x] **Karen's own daily brief keeps landing in the Unsubscribe folder.** The 2026-09-06 brief was intercepted, found by the 2026-09-07 run; **the 2026-09-07 brief was intercepted too, found by the 2026-09-08 run**. Two days running, so Karen may not have seen either. The Outlook rule needs an exception for mail from karen@kasa.ca so she stops having to fish her own brief out. Flagged 2026-09-07, recurred 2026-09-08. **2026-09-09: no interception reported.** That run found 14 Daily Brief emails, Aug 21 through Sep 8, sitting in the Inbox, and found no brief in the Unsubscribe folder. Not proof the rule was fixed, since an earlier run may simply have relocated them; watch one more morning before closing this. **2026-09-10: no interception again, second clean morning in a row.** That run pulled all 37 Unsubscribe items and found no Daily Brief among them, and found the 15 Daily Brief emails Aug 21 through Sep 9 sitting in the Inbox where they belong. The "watch one more morning" condition is met. Closed 2026-09-10; reopen only on a fresh interception
- [x] **Asana project names are missing from every task in the brief.** `daily-brief-asana` requests only `name,due_on,assignee_section.name,permalink_url`, so all 38 Asana tasks in the 2026-09-08 brief showed no project at all. Add `projects.name` to the opt_fields list. Added 2026-09-08. **Recurred 2026-09-09**: all 31 Today tasks and all 7 Recently Assigned tasks again showed either "My Tasks" or a raw numeric project id, never a project name. Second confirmed run with the same gap. **Fixed 2026-09-10**: `projects.name` added to the `get_my_tasks` opt_fields list, and the skill now says to print the real project name, or "no project" when a task has none, rather than falling back to "My Tasks" or a numeric id. Verified live the same run: the 2026-09-10 brief shows a real project name (Kelly) on the one task that has one and "no project" on the other 21. Closed 2026-09-10

## CMO: website-repo

Resolved 2026-09-07 by Karen: the trial is **30 days** (7 and 30 were both
tested, 30 is current) and the library is **6,700+ stretches**. Both were
swept across every page, `src/data/site.ts`, the FAQ data and the four blog
posts that stated an old figure, and the dead `seven-day` pricing variant was
removed from the homepage. Em dashes were also swept out of all three repos,
excluding scraped competitor material and vendored `.agents/` packages.

Also resolved 2026-09-07 by Karen: the site shows **$9.99 and $59.99 only**
(quarterly is coming back on future landing pages, not on the site now), so
the stale $49.99/$17.99/$8.49 PRO FAQ was corrected. `/free-7day-trial/` is an
**inactive landing page**, left as-is on purpose, not a clone to fix. A shared
`TrialReassurance` component now puts the same three lines under the CTA on
every paywall section (`/`, `/how-it-works/`, `/review/`, `/free-trial/`,
`/30-day/`).
Sits in `Ready/` but has 4 open items in its own tracker (folder location
may need revisiting, Ready usually means done, this isn't). Full detail:
`Team/CMO/Ready/website-repo/USER_TODO.md`.
- [ ] Meta Conversions API access token needs a secure home (not this repo). No backend exists yet to hold it; **recommend rotating it**, since Karen shared it in chat rather than a secrets vault. Flagged 2026-08-21
- [ ] DNS/domain cutover plan + access. Karen confirmed 2026-08-21 this is easy and ready whenever needed; window still TBD
- [ ] Remove dev-site noindex block before going live (intentionally deferred to launch, not blocked)
- [ ] Final content review pass vs. live site
- [ ] **Karen to review the cold-visitor homepage copy mock-up** at `/mock-up/` (`Team/CMO/Ready/website-repo/src/pages/mock-up.astro`, flagged 2026-09-05). Nothing on the real homepage changed. Decide: adopt in full, adopt the headline only (3 options given on the page), or drop it. Both the page and the temporary "Mock Up" nav item in `src/components/Header.astro` get deleted once decided.
- [ ] **Trial reminder emails on day 20 and day 25 have to actually exist before the mock-up copy ships, added 2026-09-05 at Karen's request.** The revised `/mock-up/` copy now promises them in four places (hero, billing FAQ, offer footnote, final CTA) because Karen heard we should tell people about them. Nobody has confirmed these two emails are built and firing. If the copy ships and the emails do not exist, it creates the exact surprise-billing complaint the objection section was written to answer (`Team/CMO/Ready/website-repo`)
- [ ] **The day 20 and day 25 reminder emails are now promised on five pages, escalated 2026-09-07.** They were previously promised only on the un-shipped `/mock-up/`. The `TrialReassurance` component puts that promise on the homepage, `/how-it-works/`, `/review/`, `/free-trial/` and `/30-day/`, at Karen's request. Nobody has confirmed the two emails are built and firing. Confirm before this goes live, or remove the third line from `src/components/TrialReassurance.astro` (`Team/CMO/Ready/website-repo`)
- [ ] **Karen to read the three rewritten pages, flagged 2026-09-07.** `/how-it-works/`, `/review/` and `/free-trial/` were rewritten to the Strategic Thesis. Images are placeholders, Karen is supplying the right ones (`Team/CMO/Ready/website-repo`)
- [ ] **Reviews are unattributed on `/review/`, and usernames do exist, flagged 2026-09-07.** The 15 quotes on `/review/` run with no name and no source. `/30-day/` already shows six of the same-style reviews WITH usernames (erinstretches, Funkymonkey9, BrockPaul95, Maman de trois, Victoria, Gamma god), so the attribution data exists somewhere. Find where those came from and add `name`/`source` to the `items` array in `src/pages/review.astro`. Do not invent any of it (`Team/CMO/Ready/website-repo`)
- [ ] **Name the real cancellation path on `/free-trial/`, flagged 2026-09-07.** The rewritten page has a plain terms table, and the "How to stop" row says you can cancel inside the seven days without naming where. Confirm the actual self-serve path for a Stripe-checkout subscription (portal link, in-app, or email) and state it. Related unresolved question: whether the iOS app sells the trial through StoreKit at all, or only through the website (`Team/CMO/Ready/website-repo`)
- [ ] **Images on the three rewritten pages do not match the copy, flagged 2026-09-07.** Karen said she would supply the right images. Two placeholders are marked with TODO comments in `src/pages/how-it-works.astro`: the physiotherapist section wants a clinical/mapping image rather than a lifestyle stretch photo, and the Ada section wants a short loop of Ada actually moving through a stretch, which is the entire point of that section (`Team/CMO/Ready/website-repo`)
- [ ] **One westretch.ca URL is noindexed in both the HTML and the HTTP header, surfaced 2026-09-08.** An Ahrefs alert ("Noindex in HTML and HTTP header [New]: 1 URL") caught by the daily brief. That URL is blocked from search entirely. Identify which page it is and confirm whether it's the intentional dev-site block already tracked above or an accidental one on a live page (`Team/CMO/Ready/website-repo`)

Resolved 2026-08-21 (contact form: keep mailto:, future swap to a
WeStretch webhook API once Karen builds it, and as of 2026-09-09 the
global footer newsletter signup shares that same mailto: fallback, so
two forms swap over when that API lands, not one; analytics/GA4/Meta Pixel IDs
provided and wired; Stripe links confirmed live; `/signup/` now redirects
home; Sign In URL confirmed correct; social links confirmed; no video
needed) and 2026-08-28 (image self-hosting: all images self-hosted under
`public/images/`, duplicates deduped). See `USER_TODO.md` for detail,
removed from this list per rule 3.

## CMO: App Store Specialist pending Manager review
- [ ] Small Business Program enrollment checklist. Verifies WeStretch is actually enrolled for the reduced 15% App Store commission (vs. 30% standard); requires App Store Connect Account Holder access to actually check, which this automation doesn't have. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-08-21-small-business-program-checklist.md`, flagged 2026-08-21, manual test run of app-store-specialist-nightly-action)
- [ ] Billing Grace Period & App Store Server Notifications checklist. Split into a quick App Store Connect config step (Account Holder/Admin) and a real backend engineering task (webhook endpoint outside this repo's scope). (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-08-21-billing-grace-period-and-server-notifications-checklist.md`, flagged 2026-08-21, first scheduled nightly-action run)
- [ ] PPO test plan for the first 3 screenshots. Brief for the Image Creation/Copywriting pipelines to produce 3 headline treatments for screenshot 1 (reusing already-graded copy), plus a full Product Page Optimization test setup ready for App Store Connect. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-08-22-ppo-test-first-3-screenshots.md`, flagged 2026-08-22, scheduled nightly-action run)
- [ ] Custom Product Pages plan (back pain, stiffness, mobility, golf, pickleball). 5 pages: keyword fields, promotional text, and screenshot-1 direction drafted for each (back pain reuses already-graded copy outright; mobility has a candidate transplant needing re-grading; stiffness/golf/pickleball need a fresh copy-loop run); plus an open question for Karen on whether to merge the stiffness and mobility pages given intent overlap. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-08-23-cpp-plan-5-pages.md`, flagged 2026-08-23, scheduled nightly-action run)
- [ ] Localize App Store assets (French, Spanish). French/Spanish translations of the screenshot and app-preview-video copy (the only English App Store asset copy that exists yet); flags that product page metadata (name/subtitle/keywords/description) has no English source to localize, and adds a new prerequisite backlog item to draft it. **Unblocked 2026-09-06:** English metadata does exist (it was live on the App Store all along) and is now captured in the repo; see the 2026-09-06 metadata item below. The French/Spanish metadata localization can proceed once Karen picks which version of each field to use. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-08-26-localize-app-store-assets-fr-es.md`, flagged 2026-08-26, scheduled nightly-action run)
- [ ] Native rating prompts implementation plan. StoreKit `requestReview` wired to streak-milestone and Nth-completed-routine moments (never onboarding/paywall/error flows), no custom pre-prompt gate, Apple enforces the 3-prompts/365-day cap silently; needs the native iOS app codebase (not in this repo) to actually implement. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-08-28-native-rating-prompts-plan.md`, flagged 2026-08-28, scheduled nightly-action run)
- [ ] **Reliability gap:** the 2026-08-24 and 2026-08-25 scheduled nightly runs both show a "Starting scheduled nightly-action run" line in `Apple Opportunity Radar/state/last-run.log` but no completion banner, no exit-code line, and no `Output/` file for either date; they appear to have started and then failed or hung silently. Needs investigation (check Windows Task Scheduler history for those two runs) before trusting the schedule unattended again.
- [ ] In-App Event draft: "7-Day Mobility Challenge." Full metadata draft (event name/short/long description, all under Apple's char limits), Challenge event type, compliance check against Apple's event-metadata restrictions, 7-day content concept guided by Ada, and an artwork brief routed to the Image Creation pipeline. Open items: exact start date, confirm the "Day N unlocks" mechanic is buildable, pick long-description option A/B. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-08-29-in-app-event-7-day-mobility-challenge.md`, flagged 2026-08-29, scheduled nightly-action run)
- [ ] Subscription offers (introductory/promotional/win-back) configuration checklist. Blocked on one open question first: does WeStretch's iOS app sell subscriptions via StoreKit/IAP at all, or only via the website's Stripe checkout (Monthly $9.99/mo, Annual $59.99/yr, both with a 7-day free trial)? **Unblocked 2026-09-07: yes, WeStretch does sell through Apple IAP** (Premium Monthly $9.99 / Yearly $59.99 / Quarterly $20.99, plus coin and streak-saver consumables, all read off the live storefront), so introductory / promotional / win-back offers are genuinely available and this checklist can be worked as written. Recommends mirroring the proven 7-day-free-trial as the introductory offer, a promotional offer for the cancel flow, and a win-back offer targeting lapsed Annual subscribers. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-08-30-subscription-offers-config-checklist.md`, flagged 2026-08-30, scheduled nightly-action run)
- [ ] Accessibility, account deletion, privacy disclosures, and subscription-clarity checklist. Six sections (accessibility in-app, Accessibility Nutrition Labels, privacy disclosures, ATT/SDK audit, account deletion, subscription clarity) for whoever owns App Store Connect + the iOS codebase to work through and check off; this automation has no access to verify any of it directly. Notes overlap with the separate SDK-fingerprinting-audit and iOS-18-deployment-target backlog items. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-08-31-accessibility-privacy-subscription-clarity-checklist.md`, flagged 2026-08-31, scheduled nightly-action run)
- [ ] Apple Featuring Nomination draft. Pitch copy for the "7-Day Mobility Challenge" In-App Event, mapped to Apple's stated evaluation criteria (innovation, UX, accessibility, localization, product page quality); explicitly flags accessibility and localization as design intent/partial-only, not compliance claims, until their own open backlog items close. Blocked on Karen picking the event start date before it's submission-ready. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-01-featuring-nomination-draft.md`, flagged 2026-09-01, scheduled nightly-action run)
- [ ] **Time-boxed, needs Apple Developer Account Holder, deadline 2026-10-01:** Apple is moving all EU developers to unified EU business terms on 2026-10-01, which requires agreeing to an updated Apple Developer Program License Agreement. Someone with Account Holder access needs to check App Store Connect → Business for a pending agreement and decide before that date; Apple does not document what happens to EU distribution if no action is taken. Same visit should settle the still-unverified Small Business Program enrollment (that one fact decides whether WeStretch's EU rate is 15% or 26%). Do NOT accept any alternative-payment or external-purchase-link entitlement; the recommendation is to stay on Apple IAP and the choice locks for 12 months. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-05-eu-business-terms-economics-evaluation.md`, flagged 2026-09-05, scheduled nightly-action run)
- [ ] EU business terms economics evaluation. Answer: **no, the EU terms do not change WeStretch's economics.** The "10% EU alternative terms" premise the backlog item was seeded on is obsolete; that addendum is discontinued. Under the unified EU terms a Small Business Program member pays 15% via Apple IAP in the EU, identical to the rest of the world. The remaining 10% route (own payment processing or link-out) nets roughly the same after Stripe fees while adding EU VAT liability, monthly reporting to Apple, and a 12-month lock-in; recommendation is to stay on Apple IAP. Also corrected the now-wrong EU bullet in the Knowledge Base and flagged two untracked Apple source URLs for the next monthly refresh. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-05-eu-business-terms-economics-evaluation.md`, flagged 2026-09-05, scheduled nightly-action run)
- [ ] Reviewer-permission process for quoting App Store reviews in marketing. Standing process (not a one-off task): how to request/record reviewer consent via App Store Connect review replies before quoting review text anywhere in marketing, plus a separate rule that star-rating claims must be re-checked against the live current rating each time they're reused. Recommends creating a reviewer-consent-log tracking file once a first quote is actually cleared for use (none exists yet, none created since no marketing asset currently uses a review quote). (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-02-reviewer-permission-process-for-quoting-reviews.md`, flagged 2026-09-02, scheduled nightly-action run)
- [ ] iOS 18+ deployment target checklist (blocks deep-linked Custom Product Pages). Verification-shaped item this automation can't confirm directly: no App Store Connect or native iOS project access; the `Westretch-UX` repo in this project is a Vite/Firebase web codebase with no iOS build config, and `UNITY-HANDOFF.md` there has no deployment-target mentions either. Checklist covers: where to find the minimum deployment target (native Xcode, Unity Player Settings, or cross-platform wrapper config), the iOS 18.0 pass condition, what to do if it's below 18.0, and updating the 5 CPP deep-link targets from the 2026-08-23 plan once resolved. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-03-ios-18-deployment-target-checklist.md`, flagged 2026-09-03, scheduled nightly-action run)
- [ ] Ad/analytics SDK device-fingerprinting audit checklist. Verification-shaped item this automation can't confirm directly: no App Store Connect or production app dependency-manifest access; the only app-shaped codebase in this repo (`Westretch-UX`) is a static UX prototype with no ad/analytics SDKs. Checklist covers: listing every SDK actually compiled into the shipping app, checking each for fingerprint-derived ID fallbacks or hashed-PII tracking identifiers, confirming ATT gating in code, and confirming SDK privacy manifests (`PrivacyInfo.xcprivacy`) exist, addressed to whoever owns the production app repo. Notes overlap with the separate not-yet-worked "fold privacy-manifest/SDK-signature verification into this audit" backlog item. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-04-ad-analytics-sdk-fingerprinting-audit-checklist.md`, flagged 2026-09-04, scheduled nightly-action run) **Superseded 2026-09-09: folded into the SDK audit v2 item below; review that one instead, it contains everything here plus the privacy-manifest and signature rules.**
- [ ] English App Store product page metadata draft. **The item's premise was partly wrong: a full English product page is already live on the App Store** (name `WeStretch: Custom Stretches`, subtitle `Pain Relief,Stiffness,Mobility`, a 2,531-char description); the repo simply had no record of it. This run captured that live baseline into the repo (read via Apple's public iTunes Lookup API and storefront HTML, no App Store Connect needed) and drafted a revision for every field: keep the name, a recommended new subtitle plus 3 alternates, two keyword-field proposals, three promotional-text drafts (that field appears unset, and it changes without an app update), and a revised description. **Four compliance flags in the live description** need Karen's decision: an implied fall/balance outcome claim built on a real user quote, "about to change your life", "as your body is ready" (implies the app senses readiness, it does not), and "7 days risk free" which may not match any in-app offer. Also needs App Store Connect to read the current keyword field, confirm the 6,700+ pose count, and confirm written permission to quote that user. Incidental finding: **minimum iOS is 15.2, not 18**, which partly answers the open deep-linked-CPP deployment-target item. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-06-english-product-page-metadata.md`, flagged 2026-09-06, scheduled nightly-action run)
- [ ] In-app purchase / subscription showcase configuration plan. Confirmed the new Apple showcase is **not** in use (the product page still shows Apple's auto-generated top-10 list of raw internal SKU names, in Apple's order, with no descriptions). Drafts a 3-item showcase, subscriptions only with Premium Annual first, every name/description inside Apple's 35/55-char limits, plus fallback copy for the 6 consumables if Karen wants them shown. **Two findings bigger than the item itself:** (1) WeStretch *does* sell through Apple IAP, which unblocks the 2026-08-30 subscription-offers item above; (2) the store sells "Premium" Monthly/Yearly/Quarterly while the app's own onboarding spec calls the paid tier "Pro" throughout, and there is also an unexplained "Pro Quarterly" at $69.99 next to "Premium Quarterly" at $20.99. **Karen's decisions needed:** what Pro is vs Premium, whether the store should say Pro or Premium, whether to showcase the consumables, and whether "Best value" is acceptable on the annual plan. Also flags 3 live SKU names with trailing spaces, and that showcase artwork requirements need verifying in App Store Connect before this can be entered. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-07-iap-subscription-showcase-plan.md`, flagged 2026-09-07, scheduled nightly-action run)
- [ ] Dark Mode support + Dark Mode screenshot. **The item's premise was backwards, and the screenshot half is already done.** All 10 live iPhone screenshots and all 10 iPad screenshots already show a dark app UI on a dark caption band; verified by downloading every one, measuring luminance, and visually inspecting three (the "brighter" ones are sunlit lifestyle photos with a near-black app UI composited on top). Nothing to add. The question that actually matters is the inverse: **does WeStretch have a Light Mode at all?** If the app is adaptive (follows the system setting) then the product page misrepresents it for every Light Mode user and the real gap is a *Light* Mode screenshot; if it is hard-locked to dark, there is nothing to do and Apple's guidance is already satisfied. **One Info.plist key decides it** (`UIUserInterfaceStyle`), which this automation cannot read; a 5-minute checklist is in the output for whoever has the iOS project. Also ruled out a real risk: the CXO redesign prototype is dark too (11 of 12 sampled screens), so the current screenshots will not go stale when the redesign ships. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-08-dark-mode-support-and-screenshot.md`, flagged 2026-09-08, scheduled nightly-action run)
- [ ] Third-party SDK audit v2: fingerprinting + privacy manifests + SDK signatures. **Replaces the 2026-09-04 fingerprinting-only checklist above**; the two Apple rule sets cover the same dependency list, so they are now one audit. Still verification-shaped and still not confirmable from here (re-verified: no `Podfile*`, `*.xcodeproj`, `build.gradle`, `pubspec.yaml` or `PrivacyInfo.xcprivacy` anywhere in this repo or either submodule), so it is a checklist for whoever holds the iOS project. Adds the two dates that bite: **2024-05-01**, missing required-reason API declarations hard-block an App Store Connect upload; **2025-02-12**, an update that *adds* a listed SDK must ship that SDK's privacy manifest or get an ITMS-91061 rejection email. Apple names 86 SDKs; the checklist sorts them by stack (Flutter / React Native / native / Capacitor-Cordova-Unity) so step 0 is just reading the lockfile. **New and useful: the live App Store privacy label is publicly readable and gives the audit a hard pass/fail anchor**: WeStretch currently declares *no* tracking, *no* data linked to the user, and only unlinked "Other Usage Data". Anything an SDK does beyond that makes the public label wrong, which is its own removal risk. Practical timing note: a build that changes no dependencies is unlikely to be blocked today, so run this on a quiet week rather than discovering it mid-release. **Incidental finding worth its own item: WeStretch's Accessibility Nutrition Labels are entirely undeclared** (the live page says "the developer has not yet indicated which accessibility features this app supports"), so the app appears in none of the App Store's accessibility filters, a direct miss given the older-adult positioning, and something the 2026-08-31 accessibility checklist did not catch because it checked compliance, not the declaration. Nine categories are declarable; needs Karen/Manager to add it to the backlog. (`Team/CMO/In Progress/App Store Specialist/Apple Opportunity Radar/Output/2026-09-09-sdk-audit-v2-privacy-manifests-and-signatures.md`, flagged 2026-09-09, scheduled nightly-action run)

## CMO: Apple Opportunity Radar monthly refresh
- [ ] **2026-09-01 (first scheduled run, fired 7:00 AM as registered):**
  Checked 13/13 tracked developer.apple.com URLs successfully, 0 failures.
  Found real updates in 10 of 16 sections. Added 5 new backlog items.
  Notable findings worth Karen/CMO attention specifically:
  - **New product-page feature, unused:** in-app purchase/subscription
    showcase (up to 20 items, custom order), added to backlog.
  - **Timely:** In-App Events are now an explicit Apple Featuring
    Nomination pathway: WeStretch already has a drafted event ("7-Day
    Mobility Challenge," 2026-08-29) worth nominating now.
  - **PPO mechanics clarified:** only one A/B test at a time, up to 90
    days, needs 90% confidence, and icon-variant tests need all icon
    variants pre-shipped in the binary.
  - **Compliance:** Apple's "tracking" definition under ATT is broader
    than assumed, a third-party SDK combining data for ad
    targeting/measurement counts as tracking even if WeStretch doesn't use
    it that way, and in-app webviews need the same ATT prompt. Also:
    SDKs increasingly need privacy manifests/signatures. Folded into the
    existing SDK-audit backlog item rather than a new one.
  - **CTO-adjacent:** Small Business Program members under 2M first-time
    downloads get free Apple Foundation Models/Private Cloud Compute
    access; only relevant if WeStretch is using/planning on-device AI.
  - Found a better on-domain Apple Search Ads candidate URL
    (`developer.apple.com/app-store/promote/`) than the 2026-08-21 pass;
    needs human sanity-check before becoming a tracked Knowledge Base
    section (see `Implementation Spec.md` section 1).
  - Full detail: `Team/CMO/In Progress/App Store Specialist/Apple
    Opportunity Radar/Knowledge Base/apple-marketing-opportunities.md`
    changelog, and `state/monthly-refresh-log.json`.
- [ ] **2026-08-21 (manual test run, not scheduled):** Checked 13/13
  tracked developer.apple.com URLs successfully, 0 failures. Found real
  updates in 9 sections (not just wording: new stats, new rules, one hard
  compliance redline), expected for a first live pull, since the prior
  baseline was Karen's manual excerpt rather than a full page-by-page
  fetch. Added 4 new backlog items. Notable findings worth Karen/CMO
  attention specifically:
  - **Compliance redline:** device-sensor-only medical measurement claims
    (blood pressure, glucose, etc.) are flatly prohibited; never let copy
    imply this.
  - **New revenue lever:** EU alternative terms give 10% commission
    (vs. 15% Small Business Program); worth checking if EU is a
    meaningful market.
  - **New CPP fact:** deep-linked Custom Product Pages need iOS 18+.
  - 3 candidate source URLs found (TestFlight, App Clips, Apple Developer
    News) for future Knowledge Base sections; need a human sanity-check
    before being added as tracked "fact," per this skill's own caution
    rule (see `Implementation Spec.md` section 1).
  - Full detail: `Team/CMO/In Progress/App Store Specialist/Apple
    Opportunity Radar/Knowledge Base/apple-marketing-opportunities.md`
    changelog, and `state/monthly-refresh-log.json`.

## CMO: Apple Opportunity Radar (fully live 2026-08-21)

Resolved 2026-08-21: both skills built, wrapper scripts built, state files
initialized, Knowledge Base seeded with Karen's baseline, both Windows
Scheduled Tasks registered and `Ready` (monthly one needed a `.cmd`-batch-
file workaround for PowerShell quoting, see project `Memory.md` if it
ever needs redoing), Manager staleness-check rule wired into this file's
rule 8.

## CMO: Review ToDo/ (awaiting Karen's approval)
Pending: `male-actor-01-cat-pose-indoor.png`, generated by the new
`Team/CMO/skills/male-actor-01-image-generator/` skill (Male Actor 01,
outdoor pickleball-court reference, in a cat-pose stretch, indoor scene).
Awaiting Karen's sign-off before it moves out of `Review ToDo/`.

`female-actor-01-pigeon-pose-park.webp` and
`female-actor-01-pigeon-pose-park reversed.jpg` were exploratory variants
tried for the homepage hero and superseded, never separately approved,
no action needed on them.

Resolved 2026-08-21: homepage hero image locked in, three variants tried
(`female-actor-01-cobra-studio-hero.webp`, then the pigeon-pose park
photo, then `female-actor-01-pigeon-pose-warm-readable-hero.webp`), Karen
confirmed the warm-readable one is best. Deployed to
`public/images/hero-home.webp` in website-repo: cropped for tight
headroom, full width/full height kept (no bottom crop, per Karen's
instruction), resized ~7% to cover wide desktop viewports without a seam.
Text-shadow added to the overlaid hero text (`src/pages/index.astro`) for
legibility. Source files left in place in `Review ToDo/` as the CMO asset
archive copies.

## Design System (CMO brand core + CXO app skin)

Set up 2026-09-09. Projects: `Team/CMO/In Progress/Brand System/` and
`Team/CXO/In Progress/App Design System/`.
- [x] ~~DECISION NEEDED: app prototype palette does not match the brand guideline.~~ **Resolved 2026-09-09.** Karen: "red is right, green is drift. update everywhere." The Westretch-UX prototype was moved off the green system onto brand Fire Red `#FC4850` (`global.css` and `variants.json`); `npm run check` and `npm run build` clean. Change table in `Team/CXO/In Progress/Westretch-UX/Memory.md`.
- [x] ~~Component libraries are still empty.~~ **Done 2026-09-09.** 10 components built in Fire Red: 5 marketing (colour, typography, paid social, email, out of home) and 5 app (buttons, panels, tactile, forms, spacing). All carry `@dsCard` markers and are ready for `/design-sync`. **Awaiting Karen's visual review before syncing.**
- [ ] **No Claude Design project created yet.** `/design-sync` needs a design-system project on claude.ai to target. Components exist and were published for review 2026-09-09 at https://claude.ai/code/artifact/5d46afb0-2d95-4267-a20d-a75126231162 . Unblocked once Karen approves. Flagged 2026-09-09

## Voice document (CMO)

Staff-authored voice doc submitted 2026-09-09. Karen approved all three
corrections the same day. Merged into
`Team/CMO/In Progress/Brand System/core/voice.md`.
- [x] ~~DECISION: does the Pro FOMO conversion lever survive?~~ **Resolved.** Scoped, not retired. The lever stays, framed as value kept rather than value lost. New section "Progress is an asset, not a debt" with a rewrite table. "You've lost 12 days" is out, "your 12 day streak is saved" is in.
- [x] ~~DECISION: rewrite what sits under "Evolves with you".~~ **Resolved.** Phrase kept, explanation replaced with the prescriptive physiotherapist version. "Learns your body", "tracks you" and "adapts to your body" added to the avoid list.
- [x] ~~DECISION: Ada and Bruce absent from the voice doc.~~ **Resolved.** Section added. Ada is the brand voice at its warmest; Bruce is the single scoped exception to the hype restraint, in-app celebration beats only.
- [x] ~~`.agents/product-marketing.md` does not exist.~~ **Created, then cut down, 2026-09-09.** 49 skills read that path. Karen caught that the first version duplicated ~120 lines of `core/voice.md`, so it was reduced to a 51 line router: pointers plus four non-negotiable rules. The product marketing facts moved to a new authority file, `core/positioning.md`.
- [x] ~~Two competing voice sources.~~ **Resolved.** `Team/CXO/In Progress/Onboarding UX Flow Spec/Brand-Voice-Principles.md` is now a pointer with a table showing where each of Karen's five original principles landed.
- [ ] **Competitive landscape in `Team/CMO/In Progress/Brand System/core/positioning.md` is a stub.** Only Bend is named. Run the `competitor-profiling` skill and write real profiles into the CMO folder, then summarise there. Flagged 2026-09-09
- [ ] **No quantitative proof points exist anywhere in the repo.** No user counts, outcome data or testimonials. `core/positioning.md` explicitly tells skills not to invent any and to ask Karen. Worth fixing so marketing copy can cite something. Flagged 2026-09-09
- [ ] **Growth and funnel targets are not recorded.** The Goals section of `core/positioning.md` points at `Team/CGO/` and `Team/CRO/` but neither holds numbers a skill could use. Flagged 2026-09-09

## Repo: skill discovery

- [x] **Every skill in the repo is now reachable as a slash command. Closed 2026-09-10.** Karen: ".claude has to also point to all skills in the root." Before this, only the 58 vendored skills under `.agents/skills/` were junctioned into `.claude/skills/`; the 38 skills under `Team/` were not, which is the limitation the root `CLAUDE.md` used to describe as a known issue. All 38 are now junctioned, taking the invokable total to 106 (58 vendored + 38 Team + 10 this repo owns directly in `.claude/skills/`). `node scripts/sync-skill-links.mjs` creates anything missing and rewrites the `.gitignore` block; `--check` reports without changing and exits 1 if anything is wrong. **Run it after adding a skill anywhere, after installing a package, and after cloning**, since junctions are local filesystem state that a clone does not carry. Root `CLAUDE.md`, `AGENTS.md` and `NAMING-CONVENTION.md` all updated to match.
- [x] **Two similar App Store image skills now both appear in the list, and they have drifted. Closed 2026-09-10.** `.claude/skills/cmo-app-store-image-creation/SKILL.md` (a real folder this repo owns) and `Team/CMO/skills/app-store-image-creation/SKILL.md` (now junctioned in as `app-store-image-creation`) are different files with near-identical purpose. They were invisible to each other while the Team one was not discoverable; now both are invokable and it is not obvious which one to reach for. **Karen: `Team/CMO/skills/app-store-image-creation` is the real one.** The `.claude/skills/cmo-app-store-image-creation/` copy is deleted and `Team/CMO/CLAUDE.md` now points at the surviving skill. Both were the same four-step structure reworded; the only line unique to the deleted one was its "brand risk flags" step naming the "stay active, stay independent" message, which the surviving skill does not spell out. Say if that phrase should be folded in; it is otherwise recoverable from git history. (`Team/CMO/skills/app-store-image-creation`, flagged and closed 2026-09-10)

## CXO: Westretch-UX
- [ ] **Character limits set on four screen groupings 2026-09-10, on Karen's numbers**, so copy cannot overflow the space the artwork leaves: S7/S10/S11 title 46 body 96; S12/S13/S14 and S40/S65/S66 title 23 body 128; S31 title 46 body 128. Measured after `{first_name}` expands, assuming a 15-character name (her call). Live as a `copyLimits` field on each screen in `KarensPlayground.json`, enforced by `scripts/validate-demo-config.mjs`, so `npm run check` and `npm run build` now fail on an over-length line rather than letting it ship. Two fixes were needed to make existing copy pass, both worth Karen's eye: the welcome body was 98 against her 96 and was trimmed by one word ("...today's routine, we'll get you stretching."), and the six Ada screens' base copy held a reviewer description rather than copy, so it now reads `[COPY TBD]` like their chapters. **Still open:** (1) every other screen in the flow is unbudgeted, including the 28 drip-queue items and the badge template, whose 42/120 limit is written in `UNITY-HANDOFF.md` but not enforced by anything; (2) the 438 empty Ada chapter slots now have to be written to title 23, which fits about three short words, so confirm that number still looks right once real copy is attempted. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-10)
- [ ] **S40 split into three per-account-type post-routine Ada screens 2026-09-10**, on Karen's instruction ("S40, should be three nodes, post routine Ada speaking message for each account type (guest,free,pro). All three will need functioning chapters like S13"). S40 is now Guest, plus new S65 (`post-routine-speech-free`) and S66 (`post-routine-speech-pro`), each with the same 73-chapter set as S12/S13/S14 and each fed by its own do-routine screen (S41/S59/S60). **Renamed the same day on Karen's answer to match the pre-routine trio**: `post-ada-guest` (S40), `post-ada-free` (S65), `post-ada-pro` (S66), alongside `pre-ada-guest`/`free`/`pro`. `UNITY-HANDOFF.md` and `Karens-TODO.md` were updated to the new ids in the same pass; `Memory.md` entries dated before 2026-09-10 still use the retired `post-routine-speech` and are left as written, with the rename recorded in that day's entry. This settles the convention for any future account-type split: explicit tier suffix on all three, no bare-id special case. **Still open:** all 219 new chapter slots are `[COPY TBD]`, and no looping brain-exercise video asset has been supplied, so all three are still a flat `Start-Routine.png` with a text block over it. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-10)
- [ ] **Karen to walk the new whole-flow map and hand back her debugging instructions in one paste.** Built 2026-09-10 at her request ("a very easy way to view the entire flow to debug ... so it is quicker for me to debug and give you all the instructions at once"): `public/flow-map.html`, generated from `KarensPlayground.json` by `scripts/build-flow-map.mjs` (`npm run map`, also wired into `npm run build` so it cannot drift). All 63 screens as boxes, 192 links as lines, laid out left to right by how many taps a screen sits from the start; click a box for what reaches it, what it reaches, what it writes, a jump into the running prototype at that screen, and a note box; "Copy my notes" emits one S-number-keyed list. Reads `resolveDrip.ts` as well as the JSON, because the drip hub's destinations are decided in code and a JSON-only map would draw a false picture. Needs `npm run dev` running (the per-screen prototype links do not resolve from a file open). Covers `KarensPlayground.json` only, same scope as the review sheet; widening it to the other three flow files is Karen's call and has not been asked for. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-10)
- [ ] **The flow map independently confirms the parked S15-S23 branch is fully orphaned, and that is now the only thing in its problems list.** `routine-type-select` (S15) appears exactly once in the entire 507KB flow file, in its own definition; nothing anywhere links to it, so all nine screens behind it (S15-S23) are unreachable by tapping. This is the same item Karen parked on 2026-09-08 ("leave them for now I will reconnect later"), not a new fault, and it is listed further down this section. Recorded here only as the map's baseline: a clean run is **9 warnings, 0 broken links**, so any other number means something new broke. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-10)
- [ ] Hotspot misalignment fixed 2026-09-08: the phone is now laid out at a fixed 390x844 and scaled as a whole (`src/lib/usePhoneScale.ts` sets `--phone-scale` from the stage's box; `.phoneScaler` in `global.css`). The old CSS resized the frame itself, so on a short or narrow window the mock screens' text stayed at its natural size inside a smaller box and drifted off the percentage-positioned hotspot rects, which is why Karen saw them line up only at certain VS Code window sizes. Verified with headless-Chrome screenshots at four window sizes with hotspots forced visible. **Still open, needs Karen:** several image screens use 9:16 artwork (`In-The-Gym.png` 3285x5840, `In-Gym-gradient.png` 941x1672) in a 390:844 frame with `object-fit: cover`, so ~18% is cropped off the left and right; on S4 that visibly clips the right edge of the Full Body / Customized buttons. Not changed, because every existing image screen's hotspot rects were authored against the cropped render and switching to `contain` would invalidate them all. Decision needed: re-cut the artwork at 390:844 (and re-measure those rects), or leave the crop. **Committed and pushed** (submodule commit `fae33d3`, bumped in this repo), but **not deployed to `westretch-ux.web.app`**, per Karen's standing 2026-09-08 "Don't deploy". (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [x] Karen to walk the new one-page screen review sheet and hand back her changes for all 58 screens in a single paste, rather than screen-by-screen in chat. Built 2026-09-08 at her request for exactly this: `public/review.html`, generated from `KarensPlayground.json` by `scripts/build-review-sheet.mjs` (`npm run review`, also wired into `npm run build` so it can't drift). Each card carries the screen's `screenLabel`, image, current copy, collapsed chapter variants, and a note box; the page's "Copy my notes" button emits one S-number-keyed list to paste back. Sheet covers `KarensPlayground.json` only; widening it to the other three flow files is Karen's call and hasn't been asked for. Also item 7 in that project's `Karens-TODO.md`. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08) **Done 2026-09-08: she returned notes on 15 screens and they are all applied; see the item below.**
- [ ] Generic drip-queue's 28 items exist only as one representative example screen (S29) in `KarensPlayground.json`; the other 27 aren't built as real screens yet. Drafted first-pass wording for all 28 (title/subtitle/2 buttons/hyperlink/X-close/wallpaper-top-layer-animation, same sheet format as the badge template) in the "Drip Slot Schedule" Artifact's new section 3a, for Karen to confirm before they get built out individually for testing. Uniform first-pass assumptions applied to all 28 (flagged in the Artifact, not yet Karen's call): Primary button = "Continue", no secondary/hyperlink/X-close, and all wallpaper/top-layer/animation entries are placeholder descriptions (no real art made yet). (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-02)
- [ ] Item 23 of the generic drip queue ("Gym or White setting") is now resolved as a real feature description, not just a typo guess: Karen confirmed 2026-09-02 it's a Settings menu item letting the user pick a plain white 3D background environment or the existing 3D gym environment. Renamed `gymOrHomeSetting` → `backgroundEnvironmentSetting` in `KarensPlayground.json` and `DefaultRemoteConfig.v2.json`. **Still open:** `src/data/flows/westretch.json` (a separate, older flow file, not the one Karen actively tests) has the same stale "likely a typo for Gym or Home" note on its own copy of this item and was not touched this pass; low priority, flagging so it doesn't silently drift further out of sync if that file is ever revisited. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-02)
- [ ] `drip-rate-us` (S25) rebuilt 2026-09-02 into 3 real nodes: the in-app branded splash (real screenshot, now with 5 individual per-star hotspots) branching to either `drip-rate-us-native` (S25b, 4-5 stars, real native App Store popup screenshot) or a new `drip-rate-us-feedback` (S25c, 1-3 stars, low-rating hand-off to the feedback form already in Settings). `rate_us_done` is set the instant any star is tapped on the splash, regardless of which branch it leads to; only Maybe Later (no star tapped) leaves it unrated. **Still open:** (1) S25c has no screenshot yet; Karen said she'll find one and add it later, currently a bare placeholder stub; (2) all of S25/S25b/S25c's hotspot rects (including the 5 new per-star ones, an even division of the old single star-row rect) are eyeballed, not pixel-measured with Python/PIL like most other static-image screens in this file, since Python isn't installed in this environment; worth re-measuring precisely if the taps ever feel off-target or if Python becomes available again. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-02, updated 2026-09-02)
- [ ] Variable/state testing structure v1 built 2026-08-29 (`src/data/variables.json`, `src/data/testPresets.json`, `chapters` on screens, `TestStatePanel` in the left panel). Only 3 of the ~35 `State-Variables.md` variables are wired in so far (`account_type`, `routines_completed`, `first_name`), and only `welcome-guest/free/pro` have example chapters. Grow the dictionary + chapters as Karen builds out more of `KarensPlayground.json`, per the workflow in that project's `Memory.md`. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-08-29)
- [ ] Unity migration handoff doc created for Jacque (`Team/CXO/In Progress/Westretch-UX/UNITY-HANDOFF.md`) explaining the `chapters`/`when` matching logic. **Open decision needed before migration starts:** this prototype's JSON shape does not match `DefaultRemoteConfig.v2.json`'s existing production shape. Karen and Jacque need to decide whether Jacque maps this content into the existing production shape, or the production config adopts this prototype's `chapters`/`when` pattern instead. (flagged 2026-08-29)
- [ ] S7 (`welcome-guest`) chapters: routines_completed 0-15 and 36-39 are exact chapters, sourced from `Copy Drafts/guest-welcome-onboarding-copy.csv`; routines_completed 16-35 now use a real rotation pool (`evergreen_pool_index`, a new randomizable test-state variable) instead of exact chapters, per Karen's 2026-08-29 request to build out the "evergreen randomization" her S7 `notes.uxNotes` field describes. Ids 36-39 (the CSV's "Alternate"/"Conditional" rows) are still exact chapters, deliberately left for later, not yet folded into the pool. Still open: whether this pool approach (vs. the note's "3-item pool" detail) is the final mechanic, and whether/when to extend it past routine 35 or copy the pattern to S10/S11 (welcome-free/pro). (`Team/CXO/In Progress/Westretch-UX/src/data/flows/KarensPlayground.json`, flagged 2026-08-29, updated 2026-08-29)
- [ ] Badge template infrastructure built 2026-08-30 (`component: "badge"` + `BadgeVisual` in `types.ts`/`BadgeScreen.tsx`, per Karen's field spec: title/subtitle/2 buttons/hyperlink/close, each with its own destination, plus wallpaper/top-layer/animation/widget), but **S32 `badges` itself was reverted 2026-08-31 per Karen to the real-screenshot version** (`New-PRO-user-finishes-first-routine_05.png`, no longer using the badge template); see the git-restore item below. The template code is still in the codebase, just unused by any screen right now. New `badge-check` (S33) and `share-handoff` (S34) hand-off stubs still represent the backend badge check and native share sheet; `streak-progress` (S35) is still a bare placeholder Karen said she's building next. **Open, not Claude's call:** this prototype's hotspots can't route conditionally on a variable, so `badge-check` always continues to the badge screen even when `badges_earned_today` is 0, and only one example badge is shown (no real multi-badge loop). (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-08-30, updated 2026-08-31)
- [ ] Pre/post-routine pain check-ins + speech screens inserted 2026-08-31, S30 re-scoped from "hit finish" stub to the pre-routine Ada-speech screen. `pre-routine-pain-check` (S36, background matches S8's `In-Gym-gradient.png` per Karen) and `post-routine-pain-check` (S37, keeps S30's `Start-Routine.png` background) each have a placeholder 0-10 pain-scale slider with a SKIP link below it (not a real interactive control yet); `post-routine-speech` (S38) shares S37's background photo. New `pain_rating` variable added to `variables.json`, reusing the name already established in `Onboarding UX Flow Spec/State-Variables.md`. **Open, not Claude's call:** (1) neither pain-check screen is actually gated yet (no variable tracks "chose a pain-focused routine" / "first pain routine," and the post-routine one's backend trigger schedule isn't designed; (2) `routines_completed`'s increment was moved from S30 onto `post-routine-pain-check` since S30 is pre-routine now) Claude's placement choice, not confirmed with Karen; (3) neither pre- nor post-routine Ada speech copy is written yet (account_type/routines_completed/first_name-driven); (4) `post-routine-speech` (S38, confirmed 2026-08-31 by Karen to be the end-of-routine brain-exercise video screen) has no video/animation asset supplied yet, so that layer isn't built. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-08-31)
- [ ] **S32 restored from conversation memory, not git. Flag if this ever needs auditing.** Karen asked to restore S32's "different screen previously"; git history was checked thoroughly (all commits, reflog, all branches) and confirmed `badges`/S32 doesn't exist in any commit before this session's own single squashed commit, so nothing was git-recoverable. Restored instead from this session's own conversation record of the screen's prior state (the real-screenshot version, confirmed by Karen over the alternative bare-stub version)) worth knowing this wasn't a mechanical `git checkout`, in case the reconstruction missed something from the original. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-08-31)
- [ ] New `do-routine` (S39) screen inserted 2026-08-31 between the pre-routine speech (S30) and post-routine pain check-in (S37), representing the native guided-routine playback (Screen-Inventory row 15) with a baked-in "FINISHED" link, same pattern S30 itself used before its re-scope. Still a placeholder background (`Start-Routine.png`), no real guided-routine art yet. Same day: `post-routine-speech` (S38) got an `autoAdvance` (3000ms, Karen's given value, not tied to real Ada-audio timing) to `badge-check` alongside its manual Continue; `pre-routine-pain-check` (S36)'s slider moved down 5% and enlarged 30% (third layout pass same day). All verified via `npm run check`/`build` + CDP click/timing tests. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-08-31)
- [ ] S24 (`routine-end-drip-hub`) real sequencing logic built 2026-09-01, slot table corrected 2026-09-02: `resolveDrip.ts` (`advanceDrip`/`confirmDripShown`) plus two engine hotspot actions (`resolve-drip`, `confirm-drip-shown`) implement the slot 1-14 table, the every-6-slots 13/14 repeat past 14, the 3-drips/day cap, and the shown/not-shown catch-up Karen asked for (a chosen-but-unconfirmed slot stays pending and gets re-shown before anything newer if the app closes first). **2026-09-02 correction round** (Karen reviewed the case-order Artifact and sent back fixes by letter, overriding several 2026-09-01 picks): Guest slot 2 is now unconditional Rate Us; Guest 3/5/8/10/12 and Free 4/8/10/12 gained a "Rate Us while unrated" fallback; Guest slot 6 is now unconditional Sign Up (was Sign In 3/Rate Us fallback); Free slots 3/5 are now unconditional Paywall (the 2026-09-01 "falls back to Rate Us while unrated" was wrong); Free slot 6's Rate Us-while-unrated assumption was confirmed as-is (resolving the prior open flag); slot 7 (both tiers) keeps its screen type but gained copy direction (Guest: reward vs. guest-limits contrast; Free: "what Free looks like going forward" preview + Start Pro CTA (both still placeholder copy, not written). Verified both times with a pure-function test (55 checks after the correction) and a live CDP click-test against a production build (16 checks), all passing; also fixed a pre-existing gap where `TestStatePanel.tsx` never actually rendered a control for `type: "boolean"` variables. The case-order Artifact was updated in place (same link) to match. **Open, not Karen's call yet / flagged in S24's own `openQuestions`:** (1) the model assumes only one numbered slot (or the notification opt-in) can ever be pending at once) a new one is only chosen once the last is confirmed shown; if slots should keep advancing regardless (a real multi-item backlog), this needs a different data shape since `TestState` only holds flat values, no arrays; (2) slot 7's guest-limits list and Free's "going forward" preview content both need Karen's actual copy before they're more than placeholders; (3) Karen's correction list had a blank item "j" (asked her if there was more to add. **Same day, later:** added a one-time slot 0 (before slot 1 ever fires)) Guest gets a create-an-account prompt, Free a Paywall with a trial + bonus offer (content not written yet), Pro gets nothing; has its own independent pending/shown pair (`drip_slot0_pending`/`drip_slot0_shown`) so it doesn't consume slot 1's place, reuses the existing `drip-sign-in`/`drip-paywall-hub` screens and hotspots unchanged. Also removed the "corrected 2026-09-0X" changelog-style narration from `resolveDrip.ts`'s doc comments per Karen's request (that history belongs in this tracker/Memory.md, not in the code); the Artifact's flag-list was rewritten the same way. 27 new pure-function checks added (slot 0 fires first, survives a simulated close, never refires once shown, skipped for Pro, respects the daily cap), all passing alongside the existing suite; `npm run build` clean. Added a separate plain-language to-do (design the during-routine screen S39, design the Paywall S27, revisit the Rate Us sheet S25) to `Karens-TODO.md` per her ask (not listed here since that file is explicitly her own backlog, not a Claude-flagged open item. **Same day, later still:** reversed the "slot keeps climbing past 14" decision) Karen picked the day-based alternative instead, so past slot 14 the 13/14 pair now repeats on a real day cadence (`drip_days_since_slot14`, manually advanced) rather than a climbing slot count; `drip_slot_index` freezes at 14. Confirmed the slot-0 endless-retry-until-confirmed edge case works as intended (no change needed, added a test proving it). The 28-item generic queue no longer loops (once exhausted (`generic_queue_exhausted`), further unmarked slots (pre- or post-14) go to a new bare-placeholder `leaderboard` (S42) stub instead. 27 more pure-function checks added, all passing; `npm run build` clean; Artifact and S24/S29 notes updated to match. **Open, not Karen's call yet / flagged in S24's own `openQuestions`:** (1) the single-pending-thing model, now generalized to 4 kinds (numbered slot, notification, slot 0, post-14 repeat)) still assumes only one can ever be pending at a time; (2) slot 7's guest-limits list, Free's "going forward" preview, and slot 0's trial+bonus specifics all still need real copy; (3) `leaderboard` (S42) assumes "the leaderboard screen" means the app's real Leaderboard feature, not something new (flag if that's wrong; (4) Karen's earlier blank item "j" is still unresolved. **Same day, later still:** fixed a real drift risk found while confirming the never-loop behavior survives future growth) `resolveDrip.ts` had its own hardcoded `GENERIC_QUEUE_LENGTH = 28`, separate from `variables.json`'s `generic_queue_pointer.max: 28`; now `resolveDrip.ts` reads `max` directly, so growing the queue is a one-field change. Also found `DefaultRemoteConfig.v2.json` (the separate PRD-derived config for the real, non-prototype build) still said `"loop": true` and described wrapping back to item 1 (never updated when the never-loop/Leaderboard decision was made above; fixed there too (`loop: false`, explicit `onExhausted` → Leaderboard). **New: Pro now gets its own drip cycle**, per Karen's ask) Pro already meets the has-signed-in/is-Pro bar that Guest's Sign In/Sign Up and Free's Paywall slots exist to clear, so Pro never sees any of those three screens, but can still see Rate Us while unrated and shares the same generic education queue. Added `proCase()` to `resolveDrip.ts` (slots 2/4/6/8/10/12/13 gated on `rate_us_done`, all else generic; post-14's 14-case is generic instead of a Paywall) and a matching `"pro"` section in `DefaultRemoteConfig.v2.json`. The exact slots (2/4/6/8/10/12/13) chosen for Pro's Rate Us, mirroring Free's existing pattern, was Claude's assumption going in (**Karen confirmed it 2026-09-02**, no longer open. Verified with a 12-check standalone pure-function test against the real `resolveDrip.ts` (Pro's unrated/rated walks, post-14 cadence, Guest's Sign In recurrence unaffected, exhaustion tracking `variables.json`'s configured max)) all passing; `npm run check`/`build` clean. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-01, updated 2026-09-02)
- [x] `pain-sport-mobility` (S9) built out 2026-09-02: same wallpaper as S7, 3 buttons (Reduce Pain & Stiffness / Sports Related / Improve Mobility, matching Onboarding UX Flow Spec's Screen-Inventory.md row #4 "Second"), new back-arrow badge (top-left, new reusable `showBackButton` flag) returning to S7, no login link. Each button's destination didn't exist yet, so 3 new bare stubs were added (`pain` S43, `sport` S44, `mobility` S45) rather than self-looping. **Open, not Karen's call yet:** (1) `mobility` (S45) has no corresponding row anywhere in the Onboarding UX Flow Spec's Screen-Inventory.md; flag if it needs one added there; (2) all 3 new stubs' Continue currently routes back to `welcome-guest` (S7) purely to keep the prototype loop closed, not a confirmed real destination; (3) S9's title/body copy is still `[COPY TBD]` placeholder, matching the spec row's mockup-only status. Verified with `npm run check`/`build` (clean) and a live CDP click-test confirming S7→S9, S9's back button→S7, and all 3 of S9's buttons independently reaching their new stubs. **Resolved 2026-09-05:** all three stubs replaced by the real customize path (see the entry below) - `pain` and `sport` are now real screens, `mobility` was deleted since Improve Mobility now goes straight into the path, and no stub routes back to S7 as a placeholder any more. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-02, closed 2026-09-05)
- [ ] Customize path built out 2026-09-05 from Karen's 6-point spec + 7 supplied mockups: `pain` (the screen the app numbers S45, 9 on/off pain areas), `sport` (4 choices), new `standing` / `floor` / `body-filter` / `base-positions` / `customization` screens, and the old `mobility` stub deleted. Three new config mechanisms make the branching data rather than code (`navigate-conditional`, `minVar`/`maxVar` variable-to-variable comparison, `togglesVariables`, `hiddenWhen`); 32 new variables; `validate-demo-config.mjs` now also checks every variable a rule names against `variables.json`. Verified with `npm run check`/`build` (clean), a config-level trace of all six specified paths, and a live CDP click-test (19/19). Karen reviewed and closed out the design questions on 2026-09-05: `routine_style` stays a number Unity fills in; `customize_your_routine_threshold` stays 9 (no routine-length education drip is planned); the Customization screen's Body Filter card now reads its own `show_body_filter_card` variable because Unity already owns that decision; the Body Filter screen's own controls already exist in Unity and were deliberately not re-invented here; the mockups' baked-in example selections are fine as-is for now. **What's left is Unity-side only and lives in `Jacques-TODO.md` (5 items):** linking the four sport `routine_style` numbers, matching `show_body_filter_card` to Unity's own name, linking the Body Filter screen to Unity's existing variables, reconciling the generic-drip-queue counter with `SummaryDrip`, and the screen-numbering resync below. (`Team/CXO/In Progress/Westretch-UX/Jacques-TODO.md`, flagged 2026-09-05)
- [x] Screen numbers in `KarensPlayground.json` drifted from what the app displays: `PrototypePlayer.tsx` renders a screen position, not the `screenLabel` field, and the two split by 2 when the two Rate Us sub-screens were inserted mid-array. **Resolved 2026-09-05:** Karen chose the displayed numbering, so every `screenLabel` was reset from array position and every S-number reference in the JSON notes, `Memory.md`, `UNITY-HANDOFF.md`, `Karens-TODO.md` and `Jacques-TODO.md` was rewritten to match in the same pass. `Jacques-TODO.md` item 5 closed. The underlying hazard remains and is noted there: inserting a screen mid-array still silently shifts every label after it, so the labels have to be re-synced in that same change. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-05, closed 2026-09-05)

- [ ] Real sign-in / sign-up / forgot-password chain built 2026-09-05 from mockups Karen had already supplied. S7's LOGIN link now opens `sign-in` (`screenLabel` S52, shown as S53 in the app) instead of the `auth-handoff` stub (S6), with a new config-rendered bottom link ("New here? Create An Account", her exact wording) to `sign-up` (S53, `Sign Up.png` copied to `sign-up.png` so the path has no space), plus the three reset screens `forgot-password-email` (S54), `forgot-password-code` (S55) and `forgot-password-new` (S56). Signing in, and finishing a password reset, both use `navigate-conditional`: Customization (S51) once `generic_queue_current_item` has reached `customize_your_routine_threshold`, otherwise back to S7; creating a new account always goes to S7, per Karen. Hotspot rects were pixel-measured off the source PNGs, not eyeballed. Verified with `npm run check`/`build` (clean) and a 13-check live CDP click-test covering both branches of both conditional routes. **Karen decided 2026-09-05:** (1) signing in, creating an account and finishing a password reset now all set `account_type` to `free`, with the caveat that a real sign-in could be a Pro account and the prototype cannot tell; she asked for Jacque to check it, logged as `Jacques-TODO.md` item 6; (2) the Facebook/Google/Apple wiring is approved and logged for Jacque as item 7. **Still open, needs Karen:** (3) S12's Sign In, S26's Sign In and S27's Start Free Trial still hand off to the `auth-handoff` stub, only S7's LOGIN was rewired; (4) there is no real text input anywhere in the chain, so Login always succeeds, any security code counts as correct, and no error state exists; (5) `Login_01`, `Login_03`, `Login_04` and `Login_05` are still unused by any screen, confirm whether any of them belong in this chain. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-05)
- [ ] S49 Body Filter made real 2026-09-05 on Karen's instruction: the X hotspot removed and replaced by a back chevron (`showBackButton`, `action: back`, so it returns to whichever of Floor / Base Positions / Customization the tester came from), "What's this?" repointed from the generic info panel to a new `body-filter-help` screen (S49b) built from her supplied artwork, and every control on the mockup made live - the Upper and Lower group cards plus all 12 body-part circles, each toggling its own new `body_filter_*` boolean. `Jacques-TODO.md` item 3 rewritten with the full name table for him to match against Unity. Rects pixel-measured off the PNG and verified by rendering them back over the image. `npm run check` clean. **Open, needs Karen or Jacque:** (1) the mockup still *draws* the X and still includes the phone body and wood floor around the screen, so the X looks tappable when it isn't and the back chevron sits on the bezel rather than inside the panel; a re-export (ideally cropped to the phone screen) is logged as `Karens-TODO.md` item 4, and re-cropping means re-measuring all 16 rects; (2) the mockup draws 12 circles, not the 16 L/R booleans S49's old notes had guessed, which matches `Onboarding UX Flow Spec/State-Variables.md`'s long-standing "12 booleans" - if Unity carries 16, the missing four still need adding; (3) L/R follow the labels printed on the mockup, but the figure is drawn from behind, so Jacque needs to confirm that matches Unity's own sides; (4) all 14 booleans default to `false` (matching how `standing_*` / `floor_*` were seeded) because Karen has not given starting values, though the mockup depicts Upper on and Lower off. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-05)
- [ ] Routine-end drip loop rebuilt 2026-09-05 on Karen's correction that the schedule is keyed on `routines_completed`, not on a counter of drips already shown. The old `drip_slot_index` advanced once per drip shown and capped at 3 per calendar day, so a Guest's very first routine burned slots 0, 1 and 2 back to back; that single cause produced both bugs she reported (the Sign In screen twice in a row, and Rate Us at routine 1 instead of 2). Now every routine end opens 3 slots: the first is whatever the schedule holds for the current `routines_completed`, the other two are the next education-queue items, so a Guest's first finished routine is Sign In, then queue item 1 (Meet the Leaderboard), then item 2, which is what she said should have happened. `routines_completed` 0 became a live repeatable state (routine abandoned under 75%) rather than a one-time slot 0, and the `drip_slot0_*` pair was deleted. Verified with 20 pure-function checks against the real `resolveDrip.ts` and 14 live CDP click-tests; `npm run check`/`build` clean. **Karen confirmed 2026-09-06:** 3 drips per *routine end*, replacing the previously confirmed 3-per-calendar-day cap, with no daily ceiling on top of it; settled, no longer open. **Still open, needs Karen:** (2) a user at `routines_completed` 0 gets the account ask and no education items, which she did not say either way; (3) a drip left pending at the end of one routine is confirmed against the *next* routine's `scheduled_drip_shown`, so in that rare case the new routine's own scheduled screen is skipped, the same single-pending-value limitation the old build had. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-05)
- [ ] Paywall copy drafted 2026-09-07 for Free at routines 7 and 14, per Karen. Written against `DefaultRemoteConfig.v2.json`'s `userTiers` (Free gets a 14-routine unlocked-Pro grant, then the 50-stretch `curated50` pool instead of the 6,700 `fullLibrary`), so routine 7 is the halfway preview that slot was specced for and routine 14 is the moment the grant runs out. Both chapters are gated on `account_type: "free"` as well as the count, since routine 7 for a Guest is a Sign Up screen. **Open, needs Karen:** (1) routine 7's copy states the Free grant as a flat 14, which is right for the common path but understates it for a user who signed in with unused Guest routines carrying forward; showing a live count needs a new `pro_routines_remaining` variable; (2) routine 14 claims the user keeps their streak and history when the grant runs out, which reads as obviously true but is stated nowhere in the config; (3) routine 14's "odd surprise Pro day" describes the config's `randomProDay` (10 percent, deliberately not quoted as a number); drop the line if that mechanic is not shipping; (4) routines 3 and 5 (Paywall1 vs Paywall2) and routine 0 (trial + bonus) still have no copy, and nothing yet says how Paywall1 and 2 differ. **Also worth reconciling with Jacque:** the screen is named `PayWallOutOfProDaysScreen` but the mechanic counts routines, not days. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-07)
- [ ] `DefaultRemoteConfig.v2.json`'s per-tier drip `slots` arrays disagree with `resolveDrip.ts` and were **not** reconciled in the 2026-09-05 rebuild. The semantics were updated there (`maxDripsPerRoutine`, `routinesCompleted` keying, a `slotZero` block, the notification fallback) but the schedules themselves still carry the older PRD table: guest slot 2 is generic there and Rate Us in the prototype (Karen confirmed 2026-09-05 that Rate Us belongs on routine 2), guest slot 6 is SignIn3Screen there and a SignUpScreen in the prototype, and Free's paywalls sit on different slots with different rating gates. Flagged in the file itself as `_openReconciliation` with a do-not-build-from-this warning, since it is Jacque's build config and the divergence predates this round. Needs one deliberate pass, tier by tier, with Karen confirming which table is right. (`Team/CXO/In Progress/Westretch-UX/DefaultRemoteConfig.v2.json`, flagged 2026-09-05)
- [ ] Collect Coins (S42, now "Earn a Coin") and Goal Reached (S43) gated 2026-09-05 on the education drip that explains each one, both compared against `generic_queue_current_item`. **Rebuilt 2026-09-09 on Karen's 5-point note:** both are real screens now, not stubs (`Daily Coin.png` and `goal reached.png`); the coins education moved to queue position 1 so `collect_coins_drip_threshold` is 1 and `goal_reached_drip_threshold` is 19; and Goal Reached gained a **second** gate, `minutes_stretched_today >= daily_goal_minutes`. That second gate **reverses her 2026-09-07 ruling** that the education drip was the whole gate; the old do-not-add-it-back note on S43 is marked superseded, not ignored. **Still open:** which queue item counts as "the goal reached education" is still read as "Set a Weekday Goal" (now item 19); and that item is about which *days*, not how many *minutes*, so where the user sets `daily_goal_minutes` is unanswered. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-05, reworked 2026-09-09)
- [ ] Karen's four "sign up" mockup exports are byte-identical: `First time sign up.png`, `Second time sign up.png`, `Third time sign up.png`, `On going sign up.png` and S54's `Sign Up.png` all hash the same, so the per-state copy she described never made it into the artwork. For S28 she approved painting the panel colour over the baked-in headline (`first-time-sign-up.png`, originals untouched) so her new title and subtitle could render in the gap, which covers `routines_completed` 0 and 1. **Karen supplied the second and third states' copy 2026-09-07** and asked for it improved with the marketing skills; both are built as chapters (count 4, the trial-ends reassurance, reframed from her loss-framed opener per the standing guest rule; count 6, her stiff-neck pitch). Her ordinals were mapped onto the counts a Guest actually reaches this screen at (0, 1, 4, 6, 7, 14), so second is 4 and third is 6. **Still open:** the on-going state (counts 7 and 14) has no copy and still falls back to the abstract mock; count 7 is also where the guest-limits contrast needs her actual list. Logged as `Karens-TODO.md` item 5. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-05)
- [ ] `src/data/flows/KarensPlayground.json` was found **syntactically invalid** at the start of the 2026-09-05 S49 session and had to be repaired before anything else: a stray sentence about the single-pending-drip model had been pasted into S1's `uxNotes` (destroying a `],`), and S25b and S29 were each missing a comma between two adjacent strings in a notes array. Repaired and verified. **Cause identified 2026-09-05 (later session): this was collateral from a bulk em dash sweep** that used whole-file string replacement rather than line-aligned edits, so a replacement landed on the wrong occurrence. The same pass also silently overwrote S25's five star-hotspot labels with S24's old review-only labels; that has since been repaired too, and the sweep was redone line by line against `git show HEAD:` (see this project's `Memory.md`). Nothing had flagged either problem because `validate-demo-config.mjs` cannot run on a file that will not parse, and nothing runs on save, so a broken working tree can sit unnoticed. **Worth a small guard** (a pre-commit hook, or an editor JSON validation setting) so this is caught when it happens rather than at the start of the next session. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-05)
- [ ] S7's bottom "Already have an account? LOGIN" line is hidden for Free and Pro as of 2026-09-08, per Karen: the text (`visual.linkHiddenWhen`) and the tap target (the login hotspot's `hiddenWhen`) both keyed on a new `{ "not": "guest" }` condition shape, since a `when` block is an AND and could not otherwise say "free or pro". Same day, Karen confirmed as a standing rule that signing in goes back to the start of another routine (S7), with no leaderboard drip and no leaderboard (S44) in between; recorded on `sign-in` (S53)'s own notes and in the project's `Memory.md`. **Open, needs Karen:** sign-in still routes to Customization (S52) instead of S7 once `generic_queue_current_item` has passed `customize_your_routine_threshold`, her own 2026-09-05 rule. That was read as still matching "the start of another routine" (S52 is what replaces S7 at that point, not a drip); say if sign-in should always land on S7 regardless. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [ ] S3's "Preview as Free" now lands on S7 (`welcome-guest`) with `account_type: "free"` instead of on S10 (`welcome-free`), per Karen 2026-09-08 ("it should go to S7 with the free wording"). S7 is now the one Welcome screen for all three tiers, which matches every other return path into the loop (sign-in, sign-up, password reset, Customization wizard all already came back to S7). Four of S7's chapters were guest-only sign-in nudges (routines 6, 9, 10 and evergreen pool index 4); each is now gated on `account_type: "guest"` with a signed-in sibling right after it. Verified with 96 checks against the real compiled `resolveScreen.ts` plus `npm run check`/`build`, both clean. **Open, needs Karen:** (1) the four signed-in replacement lines are Claude-drafted, not hers and not from `Copy Drafts/guest-welcome-onboarding-copy.csv` (which is guest-only), listed for review as `Karens-TODO.md` item 6a; (2) Free loses the welcome-screen "Go Pro" secondary button S10 gave it, since S7 shows Full Body / Customize to every tier; Free's routine 3/5/7/14 paywalls are untouched, but if that button should come back S7 needs a tier-conditional secondary label, which the schema does not support today (hotspots have `hiddenWhen`, `visual.secondaryAction` has no per-tier variant); **Karen decided 2026-09-08, "leave as is":** S10 stays in the file unreachable, no renumbering, no reordering of nodes into flow order; stable S-numbers are worth more than a tidy file, since they are how she and Jacque refer to screens. That sub-question is closed. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [ ] **The live prototype at `westretch-ux.web.app` is serving an old build, so Karen is reviewing stale screens.** She reported 2026-09-08 that S7 still shows the "Already have an account? LOGIN" line as a Free user. The fix itself is correct in the repo: S7 was resolved for all three tiers at routine counts 0/1/6/9/20 against the real compiled `resolveScreen.ts`, and Guest gets the link text plus the `login` hotspot while Free and Pro get neither. The local `dist/` (built 2026-09-08 11:34) has it too. The deployed bundle (`assets/index-ieTwRkBc.js`, fetched live) contains zero occurrences of `linkHiddenWhen`, so the fix was never published. Two things are also unshipped in git: the "Hide S7's login line" commit is 1 ahead of `origin/main`, and the S3 Preview-as-Free change above is still uncommitted in the working tree. **Karen was asked 2026-09-08 whether to build and deploy and answered "Don't deploy", so this is deliberately left for her or Jacques.** Whoever publishes next runs `npm run build` then `firebase deploy` (firebase CLI 15.28.1 is installed; hosting site `westretch-ux`, public dir `dist`) and pushes `main`; note that deploying also ships the four Claude-drafted signed-in chapters flagged for review in the item above. No `.firebase` deploy cache exists in the working copy, so deploys have probably been run from another machine. **Standing hazard, not a one-off:** a fix verified locally is invisible to Karen until someone deploys, so any "we fixed it but it still looks wrong" report should start by comparing the live bundle hash in `https://westretch-ux.web.app/` against local `dist/index.html`. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [ ] Buying Pro now lands somewhere, per Karen 2026-09-08: the paywall (S29) Start Free Trial hands off to native pay (S6) and comes back to a new Congrats sheet (S58, built on her `Pro sheet.png`) with `account_type` set to `pro`. S6 does double duty as the auth hand-off and the purchase hand-off, told apart by a new `purchase_pending` boolean; Get Started returns to the routine-end drip hub (S24) so the routine end finishes its remaining slots. Backing out of the purchase is an X in the top left, her call the same day, which clears the flag and carries on with the drip; `showCloseButton` had been an `imageOverlay`-only flag and `MockScreen` now renders the badge too. Karen also closed the free-trial question ("good as it is for now"): the prototype grants full Pro immediately and says nothing about billing starting, which App Store review expects somewhere in the real flow. Verified with 36 live CDP click-tests plus `npm run check`/`build`, both clean. **Needs Jacque** (`Jacques-TODO.md` item 8): where the tier really comes from after a purchase (Unity should read the entitlement rather than have this config set it), and what a declined or interrupted purchase does, which the X does not cover. **Committed and pushed** (submodule commits `5048327` and `f46bf2a`, bumped in this repo as `132882e` and `d72c395`), but **not deployed to `westretch-ux.web.app`**, per her standing 2026-09-08 "Don't deploy", so Karen will not see it live until someone runs `npm run build` and `firebase deploy`. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [x] Em dash sweep completed repo-wide 2026-09-05, on Karen's instruction to fix them rather than log them. Every file WeStretch owns is now clean: 2,586 em dashes across 170 tracked files, plus the four untracked ones alongside them, covering this repo's own docs, all of `Westretch-UX` (including `DefaultRemoteConfig.v2.json`'s 677, `KarensPlayground.json`, `types.ts`, `resolveDrip.ts`, `variables.json`, `UNITY-HANDOFF.md` and `Karens-TODO.md`) and the two files that needed hand treatment (`Onboarding-Flow.html`'s table placeholders, the radio ad's own note about removing em dashes). Replacements are a colon after a definition label, parentheses around an interruption, a semicolon before an independent clause, otherwise a comma. **Method matters, see this project's `Memory.md`:** the first attempt used whole-file string replacement and corrupted two files; the working method is to regenerate each line from `git show HEAD:` so nothing can move. **Closed by Karen 2026-09-05, do not reopen:** the vendored third-party packages under `.agents/` (10,963 across 607 skill files, plus the tool/integration docs beside them) and the scraped Bend Google Play reviews in `website-repo` (8,555 in one file) keep their em dashes permanently. They get overwritten on package updates or are other people's words held as raw evidence. Written up as a standing carve-out in root `Memory.md` under the no-em-dash rule, so this is not to be raised as a to-do again. (`Team/CXO/In Progress/Westretch-UX` plus repo-wide, flagged and closed 2026-09-05)
- [ ] **Karen's 15-screen review notes applied 2026-09-08 (afternoon).** S5 now uses the real `Home.png`; S9's `[COPY TBD]` markers removed; S10/S11 rebuilt with S7's two buttons and no bottom link; S7 forks on account type into them; S24's routine-0 line corrected **and the algorithm changed to match** (`resolveDrip.ts` no longer skips the education queue at `routines_completed` 0); S31 rebuilt on real sheet artwork with an X that ends the drip without advancing the queue pointer; S32 split into per-tier speech screens S12/S13/S14; S41 split into S41/S59/S60; S44 plus 3 new screens (Challenges S61, Notifications S62, Settings S63) share a live 5-tab bottom nav; S4 deleted. `npm run build` clean, referential integrity checked, nav rects verified against the artwork. **Committed? No. Deployed? No** (Karen's standing 2026-09-08 "Don't deploy"). (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [x] **S15-S23 are parked, Karen's call 2026-09-08: "leave them for now I will reconnect later."** Rebuilding S12/S13/S14 and re-pointing S10/S11 removed the last route into `routine-type-select` (S15), which was the only door to the pain / sport / full-body session screens (S16, S22, S23), the pain before/after measurement pair (S17, S18) and the three post-routine Ada screens (S19, S20, S21). Nothing is deleted and nothing is broken; they are still reachable by pasting the screen id into the URL hash, and their numbers are permanent now, so they will still be S15-S23 whenever she comes back to them. **Do not wire these back in, delete them, or re-raise this as a question**; Karen is reconnecting them herself later. The live post-routine path meanwhile is the other one: S41 FINISHED to S39/S40 to badge check. (`Team/CXO/In Progress/Westretch-UX`, `Karens-TODO.md` item 9a, flagged 2026-09-08) **Closed 2026-09-10: all nine deleted, on Karen's instruction after she walked the new flow map.** Her reasoning: "In the MVP we will not comment on the specific routine type special messaging." She named seven (S15, S16, S17, S18, S19, S20, S21); S22 and S23 were the last two of the same branch, reachable only from S15 and leading only to S20/S21, and she confirmed deleting them too when asked. Routine-type-specific wording is expected to return later as extra chapter conditions on the pre- and post-routine speech screens, not as separate screens. The flow now has 56 screens, 179 links, and every one of them reachable from the entry screen: the map reports zero problems for the first time.
- [ ] **Copy needed from Karen: 549 empty chapter slots across 9 screens.** The pre-routine speech (S12/S13/S14, 73 title+subtitle pairs each including first-name variants) and the during-routine speech (S41/S59/S60, 37 pairs each, no first-name variants per her instruction) are built to S7's structure and are entirely `[COPY TBD]`. This is Karens-TODO.md item 1c grown from one screen to six. Worth asking whether one line can serve several routine counts or all three tiers before she writes 330 of them; collapsing the structure now is far cheaper than filling it. (`Team/CXO/In Progress/Westretch-UX`, `Karens-TODO.md` item 8, flagged 2026-09-08) **Grew 2026-09-10** from 330 across 6 to 549 across 9: splitting S40 into a per-account-type post-routine Ada trio (S40 Guest, S65 Free, S66 Pro) added three more 73-chapter sets, all `[COPY TBD]`. Same copy gap as the pre-routine trio, tracked as `Karens-TODO.md` item 1e rather than 1c. **A first slice is drafted for her**: `Copy Drafts/post-routine-ada-guest-routines-0-5.csv`, 11 empty rows covering S40 routines 0-5 (each routine has a with-name and a no-name row, except routine 0 which has only one), so the copy can start somewhere smaller than 549 slots.
- [ ] **Screen numbers decoupled from list position 2026-09-08, on Karen's choice when asked.** `screenLabel` is now the number of record and `PrototypePlayer.tsx` renders it directly instead of computing the array index; `validate-demo-config.mjs` enforces format and uniqueness. This makes deleting a screen safe: S4 was deleted and nothing else renumbered, so the flow is S1-S3 then S5-S63 with a permanent gap at 4. Reverses the standing "never delete or move a screen" rule recorded in the project's `Memory.md` earlier the same day. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [ ] New screen-level `redirectRoutes` field added 2026-09-08 so a screen that is really a per-state fork resolves once, centrally, instead of every inbound hotspot carrying the same 3-way branch. Used by S7, S32 and S41. **S32 (`start-routine`) now covers all three `account_type` values, so it never renders: for the Unity port it is a decision, not a screen**, flagged in `Jacques-TODO.md` item 10 and `UNITY-HANDOFF.md` section 6. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [ ] **S3 (tier-select) skipped 2026-09-08 on Karen's instruction** ("assume the accounttype variable is correct and go to the appropriate S7/S10/S11 screen"). It now carries `redirectRoutes` for all three tiers, so it never renders: app launch and the auth hand-off's return both land straight on the right welcome screen. **Consequence worth knowing: the reviewer's only way to change tier is the Test State panel's `account_type` dropdown**, since the tier-picker cards are no longer reachable. They are left in place as an unreachable safety net. Also brings the prototype's launch sequence in line with the real app, where tier is derived from account state and never asked. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [ ] **S10/S11 brought to full parity with S7 2026-09-08, after Karen spotted the gap herself** ("S11 you missed making this look like S7 except the hyperlink at the bottom"). Their base visuals and buttons already matched; their `chapters` did not, still holding the old 7 thin stubs while S7 had grown to the real 0-39 set. Both now carry S7's 88 chapters minus the 8 guest-gated sign-in ones, which can never match on a signed-in screen. **Open and structural: the welcome copy now exists three times over and will drift again.** An edit to S7 does not reach S10 or S11. Either edit all three every time, or put the copy back on one screen with the tier differences as conditions, which is what it was before the 2026-09-08 split. Karen's call. (`Team/CXO/In Progress/Westretch-UX`, `Karens-TODO.md` item 1a, flagged 2026-09-08)
- [ ] **Test State presets were leaking state between runs; fixed 2026-09-08.** `applyPreset` merged a preset onto the current test state and the presets only name `account_type` and `routines_completed`, so every drip counter survived a preset change. That is what produced Karen's report that a guest's first drip after a pain routine was the Leaderboard instead of the sign-in screen: the drip schedule was correct at every routine count, but `drips_shown_this_routine` was still 3 from earlier clicking (the S31 X sets it to 3 by design). Presets now rebuild from `defaultTestState`. **Reproduced the old behaviour and the fix in test.** (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [ ] Post-routine order flipped 2026-09-08 on Karen's note "S39 should come after S40": the speech (S40) now runs before the pain check (S39). The pain gate moved off S41's FINISHED hotspot and onto S39 itself as `redirectRoutes`, because `autoAdvance` (how S40 hands over) has no conditional form. A non-pain routine passes straight through S39 to badge-check, unchanged. Also on Karen's note, Start Stretching on S5 and S44 now goes to the welcome screen (S7, forking to S10/S11) instead of dropping straight into `do-routine` with stale `routine_style` and routine length. S5's button had no hotspot at all before. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-08)
- [ ] **Generic drip queue reordered 2026-09-09 on Karen's instruction: "move the coin education to the first slot."** `coinsIntroduction` is now item 1 and every other item moved down one, in both `src/data/flows/KarensPlayground.json` (S31's 28 chapters) and `DefaultRemoteConfig.v2.json` (`routineEndDripConfig.genericDripQueue.items`). Four thresholds moved with it: `collect_coins_drip_threshold` 27 -> 1, `goal_reached_drip_threshold` 18 -> 19, `base_positions_drip_threshold` 4 -> 5, `customize_your_routine_threshold` 9 -> 10. **Worth a look:** the coins threshold is now 1, so any single education drip unlocks the Earn a Coin screen after S37; that is the literal consequence of putting coins first, not a separate decision Karen made. **The "Drip Slot Schedule" Artifact (https://claude.ai/code/artifact/c5e42671-60c8-4313-9a04-d941c739f024) was refreshed 2026-09-10** with the new order, the four moved thresholds, and the other drip changes it had fallen behind on (`routines_completed` keying, Guest-3 falling through to Sign In 2, the X on drip screens). It is the third place the queue order lives and the one Karen reads; a reorder is a three-file edit. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-09, doc refreshed 2026-09-10)
- [ ] **New screen S64 `earn-coin-daily`, the app-open Daily Login Reward, added 2026-09-09 per Karen.** S2's splash now auto-advances into it instead of into S3; it redirects itself straight on to S3 whenever `coins` is 0, and adds 1 to `coins` when the user taps Collect. **Three open questions for Karen, all three answered by her the same day** (recorded on S64/S42 in the JSON and in `Karens-TODO.md` item 10): the first routine awards the first coin; the reward is once per calendar day, via `daily_coin_collected_today`; and S42 reusing the "Daily Login Reward" artwork for a post-routine moment is fine, because Unity already owns the real post-routine sequence. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-09)
- [ ] **Four made-up variables for Jacques to map onto Unity's real ones** (`Jacques-TODO.md` item 8, added 2026-09-09): `minutes_stretched_today` (renamed from `minutes_completed` the same day, on Karen's "there is existing variables that track how many minutes are stretched today in unity. make up a variable here and Jacque can match that up to unity"), `daily_goal_minutes` (placeholder 10, accepted not confirmed), `daily_coin_collected_today` (Unity must reset it at local midnight; nothing clears it here), and `coins` (credited on tap here, should be credited on show in Unity). Nothing in this prototype can advance `minutes_stretched_today`, and by Karen's answer it does not need to. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-09)
- [ ] **Karen answered four of the five coin questions on 2026-09-09; question 3 is still open.** Answered: the first routine awards the first coin (S37 now has an ungated `routines_completed == 1` route to S42, because the drip hub runs after S42 and the education gate would otherwise skip the first routine's coin forever); the daily login reward is once per calendar day (`daily_coin_collected_today`); the minutes variable is a stand-in for Jacques; and the `daily_goal_minutes` placeholder of 10 is accepted. Question 3 was skipped in her first reply and answered in the next: S42 and S64 sharing `Daily Coin.png` is fine, "it is just art work and the unity already has a sequence", so all five are now closed. **One still open, raised by the work itself:** S42 does not set `daily_coin_collected_today`, so a user can collect the post-routine coin and the next app open's daily reward on the same day; her once-a-day rule was given about the login reward only. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-09)
- [ ] **Asked and answered 2026-09-09: is there anything in the repo about showing the customization summary screen at the start instead of S7/S10/S11?** Yes, one place: `DefaultRemoteConfig.v2.json`'s `welcomeScreenConfig` (`customizeYourRoutineThreshold`, `customizeYourRoutineScreen`, and the `_description` that spells the rule out), echoed in the uxNotes of S7/S10/S11 and in `UNITY-HANDOFF.md` / `Jacques-TODO.md` item 4. **The blocker is unchanged and is item 4's:** the real config counts that threshold in `SummaryDrip` (value 3) while the prototype counts in `generic_queue_current_item` (value 10 after the reorder), two different counters on different scales, so the rule cannot be wired end to end until Karen or Jacques says which one Unity reads. `FTUE-61-First-Time-User-Experience_01.png` is in `public/screens/westretch/` but is referenced by nothing. (`Team/CXO/In Progress/Westretch-UX`, flagged 2026-09-09)
