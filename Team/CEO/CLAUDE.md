# Team/CEO/CLAUDE.md

Scope: this folder is Karen's own operational hub — not a peer C-suite review
function like the other 10 roles, but her personal ideas, in-flight projects,
and cross-org tooling (e.g. the daily brief). Read this file (and
`Team/CEO/Memory.md`) whenever working here; cross-role conventions live in the
repo root `CLAUDE.md`.

## Mandate

Karen's own initiatives and operational tooling — things she's building for
herself or for how she runs WeStretch day to day, as distinct from the
role-specific review/planning skills the other 10 C-suite folders produce.

## Project pipeline

- `Ideas/` — early-stage concepts Karen hasn't started yet:
  - `About Me Web Site/`
  - `Build an Advisotry Board/`
  - `Nich Dashboard.txt`
- `In Progress/` — active initiatives, each with its own `CLAUDE.md` + `Memory.md`:
  - `Set up Daily housing/` — the daily brief automation (Outlook + Asana + Jira).
- `Ready/` — finished/decided initiatives, kept for reference. Empty for now.

Give each `Ideas/` concept a `CLAUDE.md` + `Memory.md` pair once it moves into
`In Progress/` and becomes real work, same as any other role.

## Memory

`Team/CEO/Memory.md` holds durable facts specific to Karen's own hub. Project-level
memory (e.g. the daily brief's calibrated behavior and state files) lives in
that project's own `Memory.md`. See the root `CLAUDE.md` "Memory" section for
the full model.
