# Approved exemplars

Empty until Karen approves a generated image (see `SKILL.md` § Pre-delivery review gate).

When one is approved, save it here with a filename describing pose + mode, e.g. `forward-fold-inside-phone.png` or `runners-lunge-trompe-loeil.png`. These become the running "closest to perfect" references this skill currently lacks; check them alongside the locked reference assets in `assets/` once any exist.
