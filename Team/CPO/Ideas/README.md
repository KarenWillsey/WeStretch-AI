# Ideas

Early-stage concepts for CPO that haven't started yet. Move a concept into `In Progress/` (as its own subfolder with a `CLAUDE.md` + `Memory.md` pair) once real work begins on it.

- `Features/` — cross-functional feature planning docs (multi-role, not yet started). See root `CLAUDE.md` "Cross-functional feature planning."
