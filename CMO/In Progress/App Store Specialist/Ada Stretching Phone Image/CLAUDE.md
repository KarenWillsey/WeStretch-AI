# Ada Stretching Phone Image — CLAUDE.md

Scope: generate the "Ada inside/beside a phone" App Store lifestyle image —
the WeStretch brand character (Ada) shown stretching, composited with a
phone. Read `Memory.md` alongside this file before generating anything.

## Where things live

- Instructions/process live in `CMO/skills/ada-stretching-phone-image/SKILL.md`
  — this folder is the project's home base (assets, output, memory), not the
  skill implementation.
- `assets/` — locked reference images (Ada identity, studio background, logo
  artwork, style examples). See `reference-map.md` for what each file is and
  why it's authoritative.
- `waiting for approval/` — in-review candidates staged as real files, not
  just chat images, so Karen can review before anything is delivered.
- `output/` — approved, delivered output only. Never place unreviewed
  candidates here (see root `Memory.md`).

## Before generating anything

Read `Memory.md` — it holds the root causes of a prior failed session and
the concrete process fixes that came out of it. Skipping it risks repeating
the same detours.
